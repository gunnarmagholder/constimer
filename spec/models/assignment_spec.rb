require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe assignment do
  before(:each) do
    
  end
  
  it "should only accept real user id's" do
    
  end
  it "should present the assignment to the manager" do
    
  end
  it "should create a valid entry only if user_id and current_user is given" do
    
  end
end